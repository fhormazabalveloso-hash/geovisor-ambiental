# Datos pesados en bruto - nunca se versionan (ver README §4)
data-raw/

# Formatos de datos SIG pesados, por si aparecen fuera de data-raw/
*.gpkg
*.tif
*.tiff
*.gdb/

# Carpeta de trabajo del pipeline (por si alguna vez se crea aqui dentro;
# normalmente vive fuera de OneDrive, ver pipeline/build_tiles.py)
pipeline/_tmp/

# Sistema / editor
.DS_Store
Thumbs.db
desktop.ini
.vscode/
